// Pestaña de Oración. El módulo vive en su contenedor; esta pantalla existe
// solo como punto de entrada de la pestaña (Documento 10).
import { OracionContenedor } from './OracionContenedor';

export function PantallaOracion() {
  return <OracionContenedor />;
}
